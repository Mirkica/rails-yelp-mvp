<div class="container">
  <h1>New review</h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.submit %>
  <% end %>

  <%= link_to 'Back', restaurant_path(@restaurant), class:"btn btn-light mt-3" %>
</div>
